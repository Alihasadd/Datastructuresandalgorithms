// Write a function, anagrams, that takes in two strings as arguments. The function should return a boolean indicating whether or not the strings are anagrams. Anagrams are strings that contain the same characters, but in any order.

const anagrams = (s1, s2) => {
    let toniBraxton = { }
  
    for ( let char of s1 ){
      if (!(char in count))
        key in object
    }
  };
  
  anagrams('restful', 'fluster'); // -> true
  
  module.exports = {
    anagrams,
  };
  
  
  // P - 2 strings
  // R - boolean true or false, if they are or are not anagrams
  // E - True and false and edge case could need to be made all lowercase
  // P - Going to take 2 strings and compare them to see if they have the same letters or not, regardless of order and return a true or false
  
  // for...of → use when you want values (like items in an array, or characters in a string).
  
  // for...in → use when you want keys (like property names in an object).